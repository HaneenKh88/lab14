<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <img src="<%= booklist.image_url %>" alt="">
        <h2><%= booklist.title %> </h2>
        <h2><%= booklist.author %></h2>
        <h4><%= booklist.isbn  %> </h4>
        <p><%= booklist.description %></p>

            <form action="/Deletebooks/<%= booklist.id %>?_method=delete" method="post">
                <button type="submit" class="btn">delete</button>
            </form>

            <button id="btn-update">Update</button>


           <section id="FormUpdate">
                <form action="/Updatebooks/<%= booklist.id %>?_method=put" method="POST" id="update-book" >
                    <input type="text" value="<%= booklist.title %>" name="title" />
                    <input type="text" value="<%= booklist.author %>" name="author" />
                    <input type="text" value="<%= booklist.image_url %>" name="image_url" />
                    <input type="text" value="<%= booklist.isbn %>" name="ibsn" />
                    <textarea name="description" cols="30" rows="10" name="description">
                        <%= booklist.description %>
                    </textarea>
                    <button type="submit">update</button>
                </form>
            </section>
       
    </div>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
<script src="../js/app.js"></script>
</body>
</html>